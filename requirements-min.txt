numpy>=1.26
pandas>=2.2
tqdm>=4.66
matplotlib>=3.8
rich>=13.7
pyyaml>=6.0
psutil>=5.9
# Optional CPU torch for windows CI; left commented for unix CI speed
# torch==2.7.1+cpu ; sys_platform == 'win32'
# --extra-index-url https://download.pytorch.org/whl/cpu