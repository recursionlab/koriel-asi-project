#!/usr/bin/env python3
"""Simple CLI wrapper for koriel package."""

import sys
import os

# Add src to path for development
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

from koriel.cli import main_cli

if __name__ == "__main__":
    sys.exit(main_cli())